#ifndef __MAHJONG_CONSOLE_H__
#define __MAHJONG_CONSOLE_H__

// The upstream GB-Mahjong repository ships console helpers outside the core
// scoring sources. The JNI bridge only needs the scoring engine, so a no-op
// compatibility header is enough here.

#endif
